# .readthedocs.yml

build:
  image: latest

python:
  version: 3.6
  pip_install: true
  setup_py_install: true
  extra_requirements:
    - docs

formats:
    - pdf
    - epub

sphinx:
  fail_on_warning: true